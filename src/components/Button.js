const Button = ({name}) =>{

    return (
        <button className="mx-[4px] my-2 py-2 px-4 border border-gray-400 cursor-pointer hover:bg-gray-500 rounded-lg">{name}</button>
    )
}
export default Button;